
return {
    -- init_options = {
      -- cmd = { "ruff-lsp"},
      -- root_dir = "~/.local/share/nvim/mason/bin/ruff-lsp",

    settings = {
        -- Any extra CLI arguments for `ruff` go here.
        args = { "--fix" },
      },
    -- },
}
